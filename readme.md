```sh
sudo apt-get install build-essential
sudo apt-get install libblas-dev liblapack-dev
sudo apt-get install python3-full
pip install pandas numpy matplotlib statsmodels pandas_datareader datetime yfinance scikit-learn PyPortfolioOpt httplib2 pandas-ta azure-storage-blob pyyaml backtesting plotly kaleido zigzag
```
